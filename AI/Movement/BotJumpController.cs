// <auto-generated>
//   AI-Refactored: BotJumpController.cs (Supreme Arbitration Overlay/Event Edition – June 2025, Ultra-Hardened, Max Realism, Zero Alloc, Canonical AIRefactored)
//   Overlay/event-only. Arbitration locked, squad/panic/suppression/anticipation aware, anti-collision. No transform/velocity set. No coroutine. Bulletproof.
//   MIT License.
// </auto-generated>

namespace AIRefactored.AI.Movement
{
    using System;
    using AIRefactored.AI.Core;
    using AIRefactored.Core;
    using AIRefactored.Pools;
    using EFT;
    using UnityEngine;

    /// <summary>
    /// Overlay/event-only, humanized jump/vault controller.
    /// Arbitration locked, pooled, squad/panic/suppression/anticipation aware, anti-collision, max-realism, multiplayer/headless safe.
    /// </summary>
    public sealed class BotJumpController
    {
        #region Constants

        private const float JumpCheckDistance = 1.08f;
        private const float JumpCooldown = 1.23f;
        private const float MaxJumpHeight = 1.19f;
        private const float MinJumpHeight = 0.32f;
        private const float ObstacleCheckRadius = 0.41f;
        private const float SafeFallHeight = 2.18f;
        private const float VaultForwardOffset = 0.73f;
        private const float HumanMistakeChance = 0.085f;
        private const float PreJumpAnticipationMin = 0.07f;
        private const float PreJumpAnticipationMax = 0.16f;
        private const float PanicCautionCooldown = 3.0f;
        private const float SuppressedCooldown = 1.7f;
        private const float SquadJumpSpacing = 0.85f;
        private const float NavSampleRadius = 0.7f;
        private const float JumpMinForwardSpeed = 0.45f;
        private const float LandingDelayMin = 0.16f;
        private const float LandingDelayMax = 0.33f;

        #endregion

        #region Fields

        private readonly BotOwner _bot;
        private readonly BotComponentCache _cache;
        private readonly MovementContext _context;
        private readonly BotPersonalityProfile _profile;

        private float _lastJumpTime;
        private float _nextAllowedJumpTime;
        private float _nextSuppressedCooldown;
        private float _preJumpAnticipationUntil;
        private float _landingDelayUntil;
        private Vector3 _queuedTarget;

        private bool _hasRecentlyJumped;
        private bool _isSuppressed;
        private bool _hasJumpTarget;

        #endregion

        #region Construction

        public BotJumpController(BotOwner bot, BotComponentCache cache)
        {
            if (!EFTPlayerUtil.IsValidBotOwner(bot) || cache == null)
                throw new ArgumentException("[BotJumpController] Invalid bot or cache.");

            var context = bot.GetPlayer?.MovementContext;
            if (context == null)
                throw new InvalidOperationException("[BotJumpController] Missing MovementContext.");

            _bot = bot;
            _cache = cache;
            _context = context;
            _profile = cache.AIRefactoredBotOwner?.PersonalityProfile ?? BotPersonalityProfile.Default;
        }

        #endregion

        #region Public API

        /// <summary>
        /// Overlay-only event. Schedules and overlays jump/vault if permitted (never sets transform).
        /// </summary>
        public void Tick(float deltaTime)
        {
            TryOverlay(deltaTime);
        }

        /// <summary>
        /// Overlay-only event. Schedules and overlays jump/vault if permitted (never sets transform).
        /// </summary>
        public void TryOverlay(float deltaTime)
        {
            const BotOverlayType OverlayType = BotOverlayType.Special;
            try
            {
                float now = Time.time;
                if (_bot == null || _cache == null || _context == null || _bot.IsDead)
                    return;

                if (_cache.IsBlinded && now < _cache.BlindUntilTime)
                    return;

                if (_isSuppressed && now < _nextSuppressedCooldown)
                    return;

                if (now < _landingDelayUntil)
                    return;

                if (now < _nextAllowedJumpTime || now < _preJumpAnticipationUntil)
                    return;

                if (!BotOverlayManager.CanIssueMove(_bot, OverlayType))
                    return;

                // Panic disables jump intent for a period, triggers suppression cooldowns
                if (_cache?.PanicHandler?.IsPanicking == true)
                {
                    _isSuppressed = true;
                    _nextSuppressedCooldown = now + PanicCautionCooldown;
                    _nextAllowedJumpTime = now + PanicCautionCooldown;
                    BotOverlayManager.ReleaseMove(_bot, OverlayType);
                    return;
                }

                // Only allow if cooldown/personality/squad/logic is satisfied
                if (!IsJumpAllowed(now))
                {
                    BotOverlayManager.ReleaseMove(_bot, OverlayType);
                    return;
                }

                // If we have a queued jump target, overlay the jump now.
                if (_hasJumpTarget)
                {
                    OverlayJump(_queuedTarget, deltaTime, now);
                    _hasJumpTarget = false;
                    BotOverlayManager.RegisterMove(_bot, OverlayType);
                    BotOverlayManager.ReleaseMove(_bot, OverlayType);
                    return;
                }

                Vector3[] temp = TempVector3Pool.Rent(1);
                try
                {
                    if (TryFindJumpTarget(out temp[0]))
                    {
                        // Human hesitation/mistake (overlay only), based on caution and personality (use only available fields).
                        float mistakeFactor = 1.0f + (_profile.Caution * 0.18f); // More cautious = more likely to hesitate
                        if (UnityEngine.Random.value < HumanMistakeChance * mistakeFactor)
                        {
                            _nextAllowedJumpTime = now + UnityEngine.Random.Range(0.18f, 0.37f);
                            BotOverlayManager.ReleaseMove(_bot, OverlayType);
                            return;
                        }
                        float anticipation = UnityEngine.Random.Range(PreJumpAnticipationMin, PreJumpAnticipationMax);
                        _preJumpAnticipationUntil = now + anticipation;
                        _nextAllowedJumpTime = now + anticipation + JumpCooldown;
                        _queuedTarget = temp[0];
                        _hasJumpTarget = true;
                        BotOverlayManager.RegisterMove(_bot, OverlayType);
                        // Next call will actually overlay the jump.
                    }
                }
                finally
                {
                    TempVector3Pool.Return(temp);
                }
                BotOverlayManager.ReleaseMove(_bot, OverlayType);
            }
            catch (Exception ex)
            {
                Plugin.LoggerInstance.LogError("[BotJumpController] TryOverlay failed: " + ex);
                BotOverlayManager.ReleaseMove(_bot, OverlayType);
            }
        }

        #endregion

        #region Internal Logic

        /// <summary>
        /// Returns true if all personality, panic, squad, and cooldown rules allow a jump overlay.
        /// </summary>
        private bool IsJumpAllowed(float now)
        {
            if (_hasRecentlyJumped && now - _lastJumpTime < JumpCooldown)
                return false;
            if (_context == null || !_context.IsGrounded || _context.IsInPronePose)
                return false;
            if (_bot.IsDead || _bot.Memory?.GoalEnemy != null)
                return false;
            if (_isSuppressed && now > _nextSuppressedCooldown)
                _isSuppressed = false;

            // No squad-stack vaulting: check for overlap in jump path.
            if (_bot.BotsGroup != null && _bot.BotsGroup.MembersCount > 1)
            {
                Vector3 myPos = _bot.Position;
                Vector3 forward = _context.TransformForwardVector;
                for (int i = 0; i < _bot.BotsGroup.MembersCount; i++)
                {
                    var mate = _bot.BotsGroup.Member(i);
                    if (mate == null || mate == _bot || mate.IsDead)
                        continue;
                    Vector3 delta = mate.Position - myPos;
                    if (Vector3.Dot(forward, delta.normalized) > 0.7f && delta.magnitude < SquadJumpSpacing)
                        return false;
                }
            }

            _hasRecentlyJumped = false;
            return true;
        }

        /// <summary>
        /// Overlays jump/vault via context (never sets transform, only animation/overlay).
        /// </summary>
        private void OverlayJump(Vector3 target, float deltaTime, float now)
        {
            try
            {
                if (_context == null)
                    return;

                _context.OnJump(); // Native overlay/animation event only
                _lastJumpTime = now;
                _hasRecentlyJumped = true;
                _landingDelayUntil = now + UnityEngine.Random.Range(LandingDelayMin, LandingDelayMax);
                BotTalkTrySay(_bot, EPhraseTrigger.GoForward); // Use closest realistic jump/vault phrase
            }
            catch (Exception ex)
            {
                Plugin.LoggerInstance.LogError("[BotJumpController] OverlayJump failed: " + ex);
            }
        }

        /// <summary>
        /// Attempts to find a valid jump/vault target in front of the bot, pooled and overlay-only.
        /// </summary>
        private bool TryFindJumpTarget(out Vector3 target)
        {
            target = Vector3.zero;
            if (_context == null)
                return false;

            Vector3 origin = _context.PlayerColliderCenter + Vector3.up * 0.24f;
            Vector3 forward = _context.TransformForwardVector;

            RaycastHit[] hits = TempRaycastHitPool.Rent(1);
            try
            {
                if (!Physics.SphereCast(origin, ObstacleCheckRadius, forward, out hits[0], JumpCheckDistance, AIRefactoredLayerMasks.ObstacleRayMask))
                    return false;

                Bounds[] bounds = TempBoundsPool.Rent(1);
                try
                {
                    bounds[0] = hits[0].collider.bounds;
                    float height = bounds[0].max.y - _context.TransformPosition.y;
                    if (height < MinJumpHeight || height > MaxJumpHeight)
                        return false;

                    Vector3 vaultPoint = bounds[0].max + (forward * VaultForwardOffset);

                    RaycastHit[] landHits = TempRaycastHitPool.Rent(1);
                    try
                    {
                        if (!Physics.Raycast(vaultPoint, Vector3.down, out landHits[0], 2.55f, AIRefactoredLayerMasks.JumpRayMask))
                            return false;

                        float fallHeight = Mathf.Abs(_context.TransformPosition.y - landHits[0].point.y);
                        if (fallHeight > SafeFallHeight)
                            return false;

                        // NavMesh check: do not allow jumps that land in invalid nav area
                        if (!UnityEngine.AI.NavMesh.SamplePosition(landHits[0].point, out var navHit, NavSampleRadius, UnityEngine.AI.NavMesh.AllAreas))
                            return false;

                        target = navHit.position;
                        return true;
                    }
                    finally
                    {
                        TempRaycastHitPool.Return(landHits);
                    }
                }
                finally
                {
                    TempBoundsPool.Return(bounds);
                }
            }
            finally
            {
                TempRaycastHitPool.Return(hits);
            }
        }

        /// <summary>
        /// Try to play a contextual bot voice line if available.
        /// </summary>
        private static void BotTalkTrySay(BotOwner bot, EPhraseTrigger phrase)
        {
            try
            {
                if (bot?.BotTalk != null)
                    bot.BotTalk.TrySay(phrase);
            }
            catch { /* Always bulletproof. */ }
        }

        #endregion
    }
}
