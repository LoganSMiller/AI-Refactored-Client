// <auto-generated>
//   AI-Refactored: TempRaycastCommandPool.cs (Ultimate Arbitration, Max-Realism, Zero-Alloc Edition – June 2025)
//   Bulletproof pooling for RaycastCommand[] arrays for high-performance AI/world batch physics logic.
//   Thread-safe, teardown/reload safe, diagnostics-ready, AI-Refactored compliant.
//   MIT License.
// </auto-generated>

namespace AIRefactored.Pools
{
    using System;
    using System.Collections.Generic;
    using UnityEngine;

    /// <summary>
    /// Pool for reusable <see cref="RaycastCommand"/> arrays for batch physics queries.
    /// Bulletproof: Thread-safe, zero-allocation, teardown/reload safe, diagnostics-ready.
    /// </summary>
    public static class TempRaycastCommandPool
    {
        private static readonly Dictionary<int, Stack<RaycastCommand[]>> PoolBySize = new Dictionary<int, Stack<RaycastCommand[]>>(16);
        private static readonly object SyncRoot = new object();
        private static int _totalRented, _totalReturned, _totalPooled;

        static TempRaycastCommandPool()
        {
            try
            {
                AppDomain.CurrentDomain.DomainUnload += (_, __) => ClearAll();
            }
            catch { }
        }

        /// <summary>
        /// Rents a pooled <see cref="RaycastCommand"/> array of at least the given size (never null, always min length 1).
        /// </summary>
        public static RaycastCommand[] Rent(int size)
        {
            if (size <= 0)
                size = 1;

            lock (SyncRoot)
            {
                if (PoolBySize.TryGetValue(size, out var stack) && stack.Count > 0)
                {
                    _totalRented++;
                    return stack.Pop();
                }
            }
            _totalRented++;
            return new RaycastCommand[size];
        }

        /// <summary>
        /// Returns a <see cref="RaycastCommand"/> array to the pool (null/zero-length ignored).
        /// </summary>
        public static void Return(RaycastCommand[] array)
        {
            if (array == null || array.Length == 0)
                return;

            lock (SyncRoot)
            {
                if (!PoolBySize.TryGetValue(array.Length, out var stack))
                {
                    stack = new Stack<RaycastCommand[]>(8);
                    PoolBySize[array.Length] = stack;
                }
                stack.Push(array);
                _totalReturned++;
                _totalPooled = stack.Count;
            }
        }

        /// <summary>
        /// Prewarms the pool with the specified number of <see cref="RaycastCommand"/> arrays of the given size.
        /// </summary>
        public static void Prewarm(int size, int count)
        {
            if (size <= 0 || count <= 0)
                return;

            lock (SyncRoot)
            {
                if (!PoolBySize.TryGetValue(size, out var stack))
                {
                    stack = new Stack<RaycastCommand[]>(count);
                    PoolBySize[size] = stack;
                }
                for (int i = 0; i < count; i++)
                    stack.Push(new RaycastCommand[size]);
                _totalPooled = stack.Count;
            }
        }

        /// <summary>
        /// Clears all pooled <see cref="RaycastCommand"/> arrays and resets pool state (teardown/reload safe).
        /// </summary>
        public static void ClearAll()
        {
            lock (SyncRoot)
            {
                foreach (var stack in PoolBySize.Values)
                    stack.Clear();
                PoolBySize.Clear();
                _totalPooled = 0;
                _totalRented = 0;
                _totalReturned = 0;
            }
        }

        /// <summary>
        /// Returns pooling stats for diagnostics/monitoring.
        /// </summary>
        public static (int arraySizes, int totalPooled, int totalRented, int totalReturned) GetStats()
        {
            lock (SyncRoot)
            {
                int pooled = 0;
                foreach (var stack in PoolBySize.Values)
                    pooled += stack.Count;
                return (PoolBySize.Count, pooled, _totalRented, _totalReturned);
            }
        }
    }
}
