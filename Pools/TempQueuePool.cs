// <auto-generated>
//   AI-Refactored: TempQueuePool.cs (Ultimate Arbitration, Max-Realism, Zero-Alloc Edition – June 2025)
//   Bulletproof, zero-alloc generic Queue<T> pooling for ultra-high-performance AI logic.
//   Thread-safe, teardown/reload safe, diagnostics-ready, AI-Refactored compliant.
//   MIT License.
// </auto-generated>

namespace AIRefactored.Pools
{
    using System;
    using System.Collections.Generic;

    /// <summary>
    /// Centralized pool for temporary <see cref="Queue{T}"/> reuse.
    /// Bulletproof: prevents heap churn in tactical and pathing logic. Thread-safe, teardown/reload safe, diagnostics-ready.
    /// </summary>
    public static class TempQueuePool
    {
        private static readonly Dictionary<Type, Stack<object>> PoolByType = new Dictionary<Type, Stack<object>>(128);
        private static readonly object SyncRoot = new object();
        private static int _totalRented, _totalReturned, _totalPooled;

        static TempQueuePool()
        {
            try
            {
                AppDomain.CurrentDomain.DomainUnload += (_, __) => ClearAll();
            }
            catch { }
        }

        /// <summary>
        /// Retrieves a cleared queue instance from the pool (never null).
        /// </summary>
        public static Queue<T> Rent<T>()
        {
            lock (SyncRoot)
            {
                if (PoolByType.TryGetValue(typeof(T), out var stack) && stack.Count > 0)
                {
                    _totalRented++;
                    return (Queue<T>)stack.Pop();
                }
            }
            _totalRented++;
            return new Queue<T>();
        }

        /// <summary>
        /// Returns a queue to the pool after clearing it. Null/duplicate returns are ignored.
        /// </summary>
        public static void Return<T>(Queue<T> queue)
        {
            if (queue == null)
                return;

            queue.Clear();
            lock (SyncRoot)
            {
                if (!PoolByType.TryGetValue(typeof(T), out var stack))
                {
                    stack = new Stack<object>(32);
                    PoolByType[typeof(T)] = stack;
                }
                stack.Push(queue);
                _totalReturned++;
                _totalPooled = stack.Count;
            }
        }

        /// <summary>
        /// Pre-warms the pool with empty queues for a given type.
        /// </summary>
        public static void Prewarm<T>(int count)
        {
            if (count <= 0)
                return;

            lock (SyncRoot)
            {
                if (!PoolByType.TryGetValue(typeof(T), out var stack))
                {
                    stack = new Stack<object>(count);
                    PoolByType[typeof(T)] = stack;
                }
                for (int i = 0; i < count; i++)
                    stack.Push(new Queue<T>());
                _totalPooled = stack.Count;
            }
        }

        /// <summary>
        /// Clears all queued instances across all types. Safe for teardown, reload, and infinite use.
        /// </summary>
        public static void ClearAll()
        {
            lock (SyncRoot)
            {
                foreach (var stack in PoolByType.Values)
                    stack.Clear();
                PoolByType.Clear();
                _totalPooled = 0;
                _totalRented = 0;
                _totalReturned = 0;
            }
        }

        /// <summary>
        /// Returns pooling stats for diagnostics/monitoring.
        /// </summary>
        public static (int typePools, int totalPooled, int totalRented, int totalReturned) GetStats()
        {
            lock (SyncRoot)
            {
                int pooled = 0;
                foreach (var stack in PoolByType.Values)
                    pooled += stack.Count;
                return (PoolByType.Count, pooled, _totalRented, _totalReturned);
            }
        }
    }
}
